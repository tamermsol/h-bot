# 2026-03-12

## TestFlight / Build Issues Resolved (from yesterday's session continued)

- **Root cause of builds not showing in TestFlight**: Builds 12-13 were uploading fine to ASC (`VALID` state) but were NOT assigned to any beta testing group. Manually assigned build 13 to "Internal Testers" group via ASC API (`12c1b517-a3ee-4c0d-bcac-d4b16cb59abb`).
- **Bundle ID confusion sorted**: `com.mb.hbot` (App ID `6760253054`, "MB Hbot") is the correct project. `com.msol.hbot` (App ID `6757429413`, "H-Bot") is a separate project. Accidentally changed to `com.msol.hbot` in build 15, then reverted back to `com.mb.hbot` in commit `ce66a8e`.
- **Future builds need beta group assignment** — codemagic.yaml publish step may not auto-assign to beta groups. Need to either fix the yaml or add a post-build API call.

## Auto-Discovery Improvements (08:45-08:58 UTC)

### Problem
iOS add-device flow required fully manual input: type WiFi SSID, password, AND full device AP name (e.g. "hbot-8857CC-6092").

### Changes Made (commit `3014506`)
1. **WiFi SSID auto-detect fixed**: `_refreshCurrentSSID()` was only checking permissions, never requesting. Now calls `WiFiPermissionService.requestPermissions()` if not granted, then retries SSID fetch.
2. **Device AP entry improved**: Pre-fills "hbot-" prefix in text field, user only types device-specific numbers. Added `textCapitalization: TextCapitalization.characters` for hex convenience.
3. **"Open WiFi Settings" button**: Uses `url_launcher` with `App-Prefs:WIFI` to jump to iOS WiFi settings. Starts AP detection timer so app auto-detects when user returns.
4. Import added: `package:url_launcher/url_launcher.dart`

### Not yet done
- Build not yet triggered on Codemagic for this commit
- Tim hasn't confirmed if he wants a build triggered
