# 2026-03-12

## TestFlight / Build Issues Resolved (from yesterday's session continued)

- **Root cause of builds not showing in TestFlight**: Builds 12-13 were uploading fine to ASC (`VALID` state) but were NOT assigned to any beta testing group. Manually assigned build 13 to "Internal Testers" group via ASC API (`12c1b517-a3ee-4c0d-bcac-d4b16cb59abb`).
- **Bundle ID confusion sorted**: `com.mb.hbot` (App ID `6760253054`, "MB Hbot") is the correct project. `com.msol.hbot` (App ID `6757429413`, "H-Bot") is a separate project. Accidentally changed to `com.msol.hbot` in build 15, then reverted back to `com.mb.hbot` in commit `ce66a8e`.
- **Future builds need beta group assignment** — codemagic.yaml publish step may not auto-assign to beta groups. Need to either fix the yaml or add a post-build API call.

## Auto-Discovery Improvements (08:45-08:58 UTC)

### Problem
iOS add-device flow required fully manual input: type WiFi SSID, password, AND full device AP name (e.g. "hbot-8857CC-6092").

### Changes Made (commit `3014506`)
1. **WiFi SSID auto-detect fixed**: `_refreshCurrentSSID()` was only checking permissions, never requesting. Now calls `WiFiPermissionService.requestPermissions()` if not granted, then retries SSID fetch.
2. **Device AP entry improved**: Pre-fills "hbot-" prefix in text field, user only types device-specific numbers. Added `textCapitalization: TextCapitalization.characters` for hex convenience.
3. **"Open WiFi Settings" button**: Uses `url_launcher` with `App-Prefs:WIFI` to jump to iOS WiFi settings. Starts AP detection timer so app auto-detects when user returns.
4. Import added: `package:url_launcher/url_launcher.dart`

### Not yet done
- Build not yet triggered on Codemagic for this commit
- Tim hasn't confirmed if he wants a build triggered

## Tuya Discussion (09:05 UTC)
- Tim asked how Tuya app does self-discovery
- Tuya uses **BLE advertisements** for device discovery (works on both iOS and Android)
- iOS cannot scan WiFi networks — Apple platform restriction, no public API
- `NEHotspotHelper` can see scan results but requires special Apple entitlement (rarely granted)
- Tasmota devices (ESP8266) don't have BLE hardware, so BLE discovery isn't an option
- ESP32-based devices could support BLE discovery

## iOS Guided Device Discovery Flow (09:11-09:40 UTC)

### Tim's idea
Skip manual entry entirely on iOS. Instead: guide user to open WiFi Settings, pick the hbot-* network, come back — app auto-detects.

### Build 16 (`69b283e4e20f8c755f197f66`)
- Replaced iOS manual entry with guided 4-step numbered flow
- "Open WiFi Settings" button as primary action
- Auto-detection timer runs in background
- Manual entry available as fallback link
- Assigned build 16 to Internal Testers beta group via ASC API

### Build 17 (`69b28a162a3e76dcfc37f9af`) — UI fixes
Tim reported two issues with build 16:
1. **Instruction text invisible** — `AppTheme.textPrimary` was white on white background. Fixed to `Color(0xFF333333)`
2. **SSID still not auto-detecting on iOS** — race condition: `_refreshCurrentSSID()` requests permissions, but then `_wifiService.getCurrentSSID()` re-checks permissions independently and still sees "not granted"
   - **Fix**: After permission granted, added 500ms delay for iOS propagation, then calls `IOSHotspotService.getCurrentSSID()` directly (bypassing service's redundant permission check)
   - Falls back to `NetworkInfo().getWifiName()` if native returns null
3. **iPad layout** — wrapped in `Center` + `ConstrainedBox(maxWidth: 500)` for proper iPad centering

### Key identifiers
- Build 16: `69b283e4e20f8c755f197f66`
- Build 17: `69b28a162a3e76dcfc37f9af`
- Internal Testers group: `12c1b517-a3ee-4c0d-bcac-d4b16cb59abb`
- App ID (ASC): `6760253054` (com.mb.hbot / MB Hbot)

### Pending
- [x] Build 17 assigned to Internal Testers
- [ ] Tim needs to test SSID auto-detect and new guided discovery UI

## SSID Auto-Detect Saga (10:00-12:15 UTC)

### Build 18 — Precise location for iOS 14+ SSID
- iOS 14+ requires **Precise Location** (not just "When In Use") for SSID reads via CNC/NEHotspot
- Added `NSLocationTemporaryUsageDescriptionDictionary` with purpose key `WifiSSIDRead` to Info.plist
- Added `Geolocator.getCurrentPosition(desiredAccuracy: high)` before SSID read
- Added native `requestPreciseLocation` method to `HotspotPlugin.swift` using `requestTemporaryFullAccuracyAuthorization`
- Build 18 assigned to Internal Testers (ID: `ed9e729c-05e4-4fbd-a9d8-5002dee27681`)

### Builds 19-21 — Compile errors
- **Build 19** (`69b29ab6bd13ef3383eb1d0d`): Failed — missing `import 'package:geolocator/geolocator.dart'`
- **Build 20** (`69b2a2dd2decafc8010638e7`): Failed — Swift error: extra trailing closure on `requestTemporaryFullAccuracyAuthorization`. API doesn't take completion handler; switched to `DispatchQueue.asyncAfter` with 3s delay.
- **Build 21** (`69b2a6a266a43c3b2767ad08`): Failed — wrong Swift arg label: `purposeKey:` → `withPurposeKey:`

### Build 22 — Compiles, but SSID still empty
- Build 22 (`69b2a8e8c5c92a11eedd5a36`) succeeded and assigned to Internal Testers
- **Still broken**: Tim's iPad on 5G cellular, NOT WiFi → no SSID to detect regardless of permissions
- The "WiFi-2.4GHz" green indicator was a **hardcoded static label**, not the detected SSID

### Build 23 — The actual fix
- **Root cause**: iPad on cellular = no WiFi connection = no SSID to detect. All the precise-location work was irrelevant.
- **Fix**: Load saved WiFi profile FIRST (SSID + password), then attempt auto-detect (overwrites if successful)
- Previously: password loaded from profile but SSID was NOT pre-filled from same profile
- Reordered `_initializePairing()`: load profile → pre-fill SSID + password → then try auto-detect
- Build 23 (`69b2ac7ea29dace2f50c8681`) triggered, ~15 min

### Lessons learned
- **Check the obvious first**: status bar showed 5G = no WiFi. Should have noticed before chasing permission issues.
- **Test assumptions**: "SSID detection not working" ≠ "permissions broken". Could just mean no WiFi.
- **No Dart SDK on server**: Cannot run `dart analyze` locally; all compile errors caught only on Codemagic. Very wasteful.
- Swift API names: `requestTemporaryFullAccuracyAuthorization(withPurposeKey:)` — `withPurposeKey` not `purposeKey`

### Build 24 — SSID overwrite bug + captive portal fix
- **Bug 1 (SSID overwrite)**: Build 23 fix was incomplete. Profile SSID loaded correctly, but `_refreshCurrentSSID()` then overwrote `_currentSSID` with null when detection failed. Also the catch block set `_currentSSID = null`. Fixed: only update `_currentSSID` if non-null SSID detected; catch block no longer clears saved value.
- **Bug 2 (Captive portal)**: iOS auto-discovery view told users to "Open WiFi Settings" → manual connect to hbot AP → iOS detects no internet → opens captive portal showing Tasmota web form. Fixed: replaced with in-app "Connect to Device" button that calls `_connectToDeviceAPiOS()` via `NEHotspotConfigurationManager` which bypasses captive portal.
- Added `_apSuffixController` for device AP suffix text field
- `joinOnce = true` already set in Swift plugin — no changes needed to HotspotPlugin.swift
- Build 24 (`69b2af9b97b02ef132679edd`) triggered
- Tim frustrated with repeated failed builds — need to be more careful with compile testing

### Key lesson: Double-check state mutations
- When loading data from multiple async sources, ensure later calls don't overwrite earlier values with null
- Pattern: load saved data → set as default → try live detection → only overwrite if successful

### Build 25 — Make saved profile SSID editable
- **Problem**: Saved profile SSID ("Momentum Sol") showed in the locked green "Auto-detected" box — couldn't edit it. Changing WiFi networks or restarting app always showed stale saved SSID.
- **Fix**: Added `_ssidFromLiveDetection` flag. Green locked box only for live-detected SSIDs. Saved profile SSIDs show in editable text field pre-filled.
- Three SSID display states now: live-detected (green locked) | saved profile (editable, pre-filled) | none (empty field)
- Build 25 (`69b2b5262328474e288cfa9e`) triggered

### Build 26 — Empty fields by default, auto-detect button
- **Tim's feedback**: Don't pre-fill from DB. Fields should start empty. Auto-detect should fill both SSID and password.
- **Changes**: Removed DB profile pre-fill from `_initializePairing()`. Fields start empty. "Auto-detect WiFi" button (full-width ElevatedButton) calls `_refreshCurrentSSID()` which detects SSID, then loads matching password from DB profile if SSID matches.
- Build 26 (`69b2b9b6a24be51b716fb22a`) triggered
- Need to assign to Internal Testers when processed

### iOS SSID auto-detect: confirmed possible but conditional
- Entitlements correct: `com.apple.developer.networking.wifi-info` + `HotspotConfiguration` both in Runner.entitlements, referenced in pbxproj
- Swift code correct: `NEHotspotNetwork.fetchCurrent` + `CNCopyCurrentNetworkInfo` fallback
- **Requirements**: Location When In Use + Precise Location ON + actually connected to WiFi (not cellular)
- Tim's iPad was on 5G (cellular) during testing — that's why auto-detect returned nothing
- **Password cannot be read on any iOS API** — only auto-fill from DB if SSID matches saved profile
- Build 26 assigned to Internal Testers (`8ce44a0c-f5de-47d0-82c3-c7a3e4851fa1`)

### Build 27 — Auto-detect button visible feedback
- Tim tested build 26: auto-detect button did nothing visible (was on WiFi this time). All debug info went to hidden debug panel.
- **Added**: `_isDetectingSSID` flag + `_detectMessage` string for visible UI feedback
- Button now shows: spinner while working, green ✅ on success, orange ⚠️ with checklist on failure, red ❌ on error
- Also loads password from DB when SSID matches saved profile
- Build 27 (`69b2c0af9359b4adb2ad1105`) triggered
- This build will reveal WHY detection fails on Tim's iPad (the message will tell us)

### Build 28 — Root cause found: Geolocator "permanently denied" blocking SSID read
- Tim tested build 27 on WiFi: log showed "Permissions permanently denied"
- **Root cause**: `Geolocator` package returned `permanentlyDenied` status (probably from initial denial, iOS remembers). Code was returning early without even trying `NEHotspotNetwork.fetchCurrent`.
- **Fix**: Permission check no longer blocks SSID read — proceeds anyway and lets the iOS API itself decide
- **Added**: "Open App Settings" button when detection fails, clear checklist instructions
- Build 28 (`69b2c1b9393c86f08357602c`) succeeded, but hadn't landed in ASC yet at 13:45 UTC
- Build 27 assigned to Internal Testers (`a7ac52b7-93b4-4493-b5f9-d9eab797998f`) as interim test while waiting for 28
- **Lesson**: Don't gate API calls behind permission_handler/Geolocator status checks on iOS — the underlying API (NEHotspotNetwork) will return nil itself if permissions are wrong. Better to just try and report the result.
- Build 28 assigned to Internal Testers (`7daad4c0-4e8b-46d1-89fc-a886e11de71e`)
- Build 27 had a bug: spinner stuck forever because early return on "permanently denied" didn't reset `_isDetectingSSID`
- Tim tested build 28: **auto-detect SSID works now!** 🎉 But captive portal popup returned when connecting to device AP
- **Captive portal issue**: `NEHotspotConfigurationManager.apply()` triggers iOS captive portal detection. Tasmota web server on 192.168.4.1 responds to HTTP, so iOS shows captive portal sheet. **No public iOS API to disable this.**
- Options: (1) tell user to dismiss popup (2) proceed anyway since connection IS active (3) Tasmota `WebServer 0` to disable web server before join
- Build 29 (`69b2c8220593005bd7e4d105`): Added captive portal dismiss instructions in status message, increased delay from 2s to 4s after AP join for iOS routing stabilization
- Need to assign build 29 to testers when processed

### CRITICAL: Captive portal popup is NOT acceptable
- Tim: "users of my hardware should not see anything related to tasmota or mqtt — definitely harmful"
- The captive portal shows the raw Tasmota web interface — exposes underlying firmware/brand to end users
- **Must eliminate captive portal entirely**, not just tell user to dismiss it
- Possible real solutions:
  1. **Don't use `NEHotspotConfigurationManager`** — use manual Settings→WiFi guided flow instead (no popup)
  2. **Pre-flash Tasmota with `WebServer 0`** in AP mode — no web server = no captive portal trigger
  3. **Custom firmware** that doesn't serve HTTP on AP mode (only responds to specific API endpoints)
  4. **BLE provisioning** — avoid WiFi AP entirely (requires hardware BLE support)
- This is a UX/branding requirement, not just convenience. Must be solved properly.

### Build 30 — Guided WiFi Settings flow (captive portal fix)
- **Removed `NEHotspotConfigurationManager` from iOS device AP connection flow entirely**
- Replaced with guided flow: "Open WiFi Settings" button → user manually taps hbot-xxx → return to app → auto-detect timer picks it up
- No programmatic join = no captive portal popup = no Tasmota exposure
- Instructions: 4 steps (pairing mode → open settings → connect to hbot-xxx → return to app)
- AP detection timer polls every 5s, auto-proceeds when connected
- Build 30 (`69b302de11e4c7e1e810d473`) — needs TestFlight assignment when processed
- Note: `App-Prefs:root=WIFI` URL scheme used to open WiFi settings directly, with `app-settings:` fallback

### Tuya self-discovery question
- Tim asked how Tuya does device self-discovery. Was researching when conversation shifted to fixing bugs.
- Answer: Tuya uses BLE for initial device discovery (device broadcasts BLE beacon), then connects via WiFi for provisioning. Also uses mDNS/Bonjour for LAN discovery of already-provisioned devices. Apple allows BLE scanning (unlike WiFi scanning). Could consider BLE beacon approach for hbot devices if hardware supports it.

## Build 31 — Open WiFi Settings button fix (18:35 UTC)
- **Bug**: "Open WiFi Settings" button stayed greyed out after first tap. `onPressed: _isLoading ? null : ...` disabled it once `_isLoading` set to true.
- **Fix**: Removed `_isLoading` guard from button — always tappable. Detection timer only starts if not already running.
- Build 31 (`69b307249387e766b0d70aad`) triggered, version `1.0.0+31`
- Commit: `02b87dc`

## Design Agent Brief (18:36 UTC)
- Tim requested a full design brief to send to a design agent
- Scanned all 34 screens, widgets, theme file, assets, navigation structure
- Wrote comprehensive brief covering: app overview, current design system (colors/tokens/fonts), all screens element-by-element, add-device flow details (4 steps), existing assets, design priorities, constraints, deliverables, reference apps
- Brief saved in conversation; Tim asked to review before saving to file
- **Key finding**: No app logo exists in assets — only `google_logo.png` for OAuth button
- **Navigation**: 3-tab bottom nav (Home, Scenes, Profile)
- **Screens**: 34 .dart files in screens/, biggest is home_dashboard (2906 lines) and add_device_flow (3111 lines)
- **Widgets**: 19 reusable widget files
- **Avatars**: 10 pre-built avatar PNGs
- **Backgrounds**: 5 default room background JPGs
