# 2026-03-16 — Build 111-113 fixes

## Build 111 (commit `c8a9124`)
- Removed debug bug_report icon from Add Device flow AppBar (Amir's feedback item #3)
- The icon was a `GestureDetector` with `Icons.bug_report` at line ~425 of `add_device_flow_screen.dart`
- Debug log still exists internally, just not visible to users
- Codemagic build: `69b7b68fb46b38b5f6a2eb90`
- ASC build ID: `3422cc5b-d445-45b8-a241-c3b6266fa89f`

## Build 112 (commit `a20041d`)
- Fixed bottom sheet disappearing on iOS in device control screen
- Root cause: MQTT `watchCombinedDeviceState` stream calling `setState()` while `showModalBottomSheet` was open
- Fix: Added `_isBottomSheetOpen` flag to skip setState during sheet display, apply pending updates on close via `.then()`
- Codemagic build: `69b7c526306422dc8cb36dde`
- ASC build ID: `2557c61a-6f06-4567-9efb-2e70e49f9443`

## Build 113 (commit `aac239b`)
- Fixed device name not saving during provisioning
- Root cause: Line 2177 in `add_device_flow_screen.dart` — `_deviceNameController.text = deviceInfo.module` was overwriting user's custom name with Tasmota module name (e.g., "Generic")
- Fix: Only set module name if controller is empty; after creation, explicitly call `renameDevice` RPC if user's name differs from DB
- Fixed yellow "no internet" banner flash on Android launch
- Root cause: Connectivity check ran immediately on `initState`, before Android network stack was ready
- Fix: Delayed initial check by 3 seconds in `home_screen.dart`
- Codemagic build: `69b7ccc51bfb39ba45b2c6ef`
- ASC build ID: `1788cac9-9568-4b57-a87a-019e8441154a`

## Key identifiers
- Latest branch commit: `aac239b` on `hbot-design`
- All builds 111-113 assigned to internal TestFlight testers
- Tim confirmed "device already linked to another account" error is resolved on his end (Supabase side)

## Patterns learned
- MQTT state streams + `showModalBottomSheet` = iOS dismissal bug. Use `_isBottomSheetOpen` flag pattern for any future bottom sheets in screens with live MQTT streams
- Tasmota device discovery overwrites text controllers — always check if user has entered custom data before auto-filling
- Android connectivity checks can fail briefly on cold start — delay initial check

## Build 114 (commit `8e44408`)
- Added Alexa skill linking: Profile → Integrations → "Connect to Alexa"
- Uses `url_launcher` to deep-link to Alexa app or Amazon skill page
- Alexa skill URL: `https://www.amazon.com/Amir-Aboelezz-Hbot/dp/B0GBZ7XB1N`
- `_openAlexaSkill` method tries `alexa://` URI first, falls back to Amazon web URL
- APK sent to Tim

## Build 115 (commit `31fcd8e`) — Features 1, 2, 3, 11
### #1 Push Notifications (local)
- `lib/services/notification_service.dart` — `flutter_local_notifications` package
- Android channels: `hbot_device_events`, `hbot_scene_events`, `hbot_system`
- iOS permission request on init
- Per-type prefs: `notif_device_offline`, `notif_device_online`, `notif_scene_run`, `notif_state_change`
- 30-second debounce for online/offline to avoid spam
- `NotificationsSettingsScreen` enhanced with per-type toggle switches

### #2 Activity Log (local SQLite)
- `lib/services/activity_log_service.dart` — `sqflite` package
- SQLite DB: `hbot_activity.db`, table `activity_log`
- `lib/screens/activity_log_screen.dart` — grouped by date, device-specific or global
- Auto-prunes to 1000 entries

### #3 Firmware OTA
- "Firmware Update" in device 3-dot menu (`device_control_screen.dart`)
- Sends Tasmota `Upgrade 1` via MQTT
- Shows current firmware version from `device.metaJson?['version']`

### #11 Home Screen Widgets (Android)
- `lib/services/home_widget_service.dart` — `home_widget` package
- `HBotDeviceWidget.kt` — Android `HomeWidgetProvider` subclass
- Widget layout: `android/app/src/main/res/layout/hbot_device_widget.xml`
- Shows up to 4 devices with ON/OFF state + color coding
- Widget data updated when dashboard loads devices

### DeviceEventTracker
- `lib/services/device_event_tracker.dart` — singleton wrapping both NotificationService and ActivityLogService
- Hooked into `SmartHomeService.watchCombinedDeviceState()` inside `maybeEmit()`
- 30s debounce, first-observation skip, device name registry

## Build 116 (commit `5eee206`) — Feature 12 + build fixes
### #12 Dark Mode
- `ThemeService` now supports Light/Dark/System modes (was hardcoded to light)
- `main.dart` uses `Consumer<ThemeService>` to reactively update `MaterialApp` theme
- Full dark theme in `AppTheme.darkTheme()` — AppBar, cards, dialogs, bottom sheets, switches, inputs, dividers
- Added helper methods: `getBackground()`, `getSurface()`, `getBorder()` in AppTheme
- Profile → Appearance: radio dialog (Light/Dark/System) replaces "coming soon" snackbar

### Build environment changes
- AGP bumped from 8.1.0 → 8.7.0 (required by `home_widget` Glance dependency)
- Core library desugaring enabled (`desugar_jdk_libs:2.1.4`)
- `minSdk` raised from 21 → 23 (Glance requires it)
- R8 minification + resource shrinking enabled
- ProGuard rules: `android/app/proguard-rules.pro`
- Split-per-ABI: arm64 APK is 31MB (down from 39MB fat APK; 82MB before split)
- Codemagic iOS build: `69b80bb7b0ec17b21de51ae6` (pending)
- APK `hbot-build116.apk` sent to Tim

### Files created
- `lib/services/notification_service.dart`
- `lib/services/activity_log_service.dart`
- `lib/services/device_event_tracker.dart`
- `lib/services/home_widget_service.dart`
- `lib/screens/activity_log_screen.dart`
- `android/app/src/main/kotlin/com/example/hbot/HBotDeviceWidget.kt`
- `android/app/src/main/res/layout/hbot_device_widget.xml`
- `android/app/src/main/res/xml/hbot_device_widget_info.xml`
- `android/app/proguard-rules.pro`

### Files modified
- `pubspec.yaml` — added `flutter_local_notifications`, `sqflite`, `home_widget`, `path`
- `lib/main.dart` — Consumer<ThemeService>, darkTheme
- `lib/theme/app_theme.dart` — full darkTheme(), helper methods
- `lib/services/theme_service.dart` — ThemeMode.system support
- `lib/screens/profile_screen.dart` — Alexa link, dark mode dialog, Provider import
- `lib/screens/notifications_settings_screen.dart` — per-type toggles
- `lib/screens/device_control_screen.dart` — firmware OTA, activity log
- `lib/screens/home_dashboard_screen.dart` — widget update, event tracker
- `lib/services/smart_home_service.dart` — event tracker hook
- `android/app/build.gradle.kts` — AGP 8.7.0, desugaring, R8, minSdk 23
- `android/settings.gradle.kts` — AGP version
- `android/app/src/main/AndroidManifest.xml` — widget receiver

### Lessons learned
- `home_widget` pulls in `androidx.glance` which requires AGP 8.6.0+ and minSdk 23
- `flutter_local_notifications` requires core library desugaring with newer AGP
- Flutter 3.24 uses `CardTheme`/`DialogTheme` (not `CardThemeData`/`DialogThemeData`)
- Fat APK with 4 ABIs doubles size; split-per-abi saves ~50%
- R8 with Flutter needs proguard rules for `SplitCompatApplication`

## Pending
- Codemagic build `69b80bb7b0ec17b21de51ae6` — waiting for iOS build result
- Feature #11 (Home Widgets) — iOS widget extension NOT yet created (only Android done)
- Navigation from notification tap (`_onNotificationTapped`) has TODO placeholder
- Scene execution notifications not wired — `notifySceneExecuted` exists but no caller

## Build 118 (commit `91b2ef2`) — Full dark mode migration
### Dark mode — 491 hardcoded color references fixed
- Created `HBotThemeContext` extension on `BuildContext` in `lib/theme/app_theme.dart`
- Helpers: `context.hCard`, `context.hTextPrimary`, `context.hTextSecondary`, `context.hTextTertiary`, `context.isDark`
- Replaced all 491 hardcoded light-mode color references across 54 files with theme-aware `context.hXxx` helpers
- Fixed 139 `const` keyword conflicts (non-const `context.hXxx` calls inside `const` widgets)
  - Automated removal of `const` from ancestor widgets containing `context.h` references
  - Some nested deeper (6-14 lines above error line) — needed expanded search radius

### Alexa skill deep link — intent URI approach
- Changed from `launchUrl('https://www.amazon.com/dp/...')` to Android intent URI
- `intent://skills/dp/B0GBZ7XB1N#Intent;package=com.amazon.dee.app;scheme=alexa;end`
- iOS: `alexa://skills/dp/B0GBZ7XB1N`

### Widget — toggle buttons + live state
- Updated `_updateHomeWidget()` to read actual MQTT state via `_mqttManager.getDeviceState()`
- Widget data now includes ON/OFF state per device
- Added `_updateHomeWidget()` call inside MQTT state change listener

## Build 119-120 — Widget and Alexa fixes (FAILED)
- Build 119: Rewrote widget to standard `AppWidgetProvider` — still "Couldn't add widget"
- Build 120: Restored `HomeWidgetProvider`, added launch intent filter + background receiver, removed dot prefix from manifest receiver name — still "Couldn't add widget"
- Alexa `alexa.amazon.com/spa/skill-store/skills/dp/ASIN` — just opens Alexa app home, NOT the skill page

## Build 121 (commit `d2c21b6`) — Proper widget + Alexa instruction sheet
### Widget fixes
- Uses direct `R.id.device_0_name` etc. instead of `getIdentifier()` lookup (which returns 0 on failure and crashes)
- Added `android/app/src/main/res/values/strings.xml` with `widget_description`
- Proper widget info XML with `minResize` dimensions
- Manifest: `HBotDeviceWidget` (no dot prefix), launch intent filter, background receiver

### Alexa — instructional bottom sheet
- Researched Amazon docs: **NO public deep link exists** to open a specific skill page inside the Alexa app
- The `alexa.amazon.com` URLs only open the app's home screen
- Replaced with `showModalBottomSheet` containing:
  - 4-step instructions (Open Alexa → More → Skills & Games → Search "H-Bot" → Enable)
  - "Open on Amazon" button as web fallback
- Used `HBotColors.primary` (not `context.hPrimary` which doesn't exist)

### Key learnings
- Android widget `getIdentifier()` returns 0 if resource not found — causes silent crash. Use direct `R.id` references
- Amazon Alexa app doesn't support deep linking to skill pages from third-party apps
- `HBotThemeContext` extension has: `hCard`, `hTextPrimary`, `hTextSecondary`, `hTextTertiary`, `isDark` — but NOT `hPrimary`. Use `HBotColors.primary` directly
- Tim's feedback: widget still failing + Alexa not linking to skill page. Build 121 is latest attempt, awaiting test results

### Codemagic builds triggered
- Build 118 iOS: `69b81bcaef5f9e16a82bdb5d`
- Build 120 iOS: `69b827cabf9399f37fcc5c94`

## Build 122 (commit `07d9c18`) — Widget ROOT CAUSE + Alexa Account Linking
### Widget — ROOT CAUSE FOUND AND FIXED
- `<View>` elements (indicator dots in widget layout) crashed `RemoteViews` inflation
- Android `RemoteViews` does NOT support plain `<View>` — only specific types: FrameLayout, LinearLayout, RelativeLayout, GridLayout, AnalogClock, Button, Chronometer, ImageButton, ImageView, ProgressBar, TextView, ViewFlipper, ListView, GridView, StackView, AdapterViewFlipper, ViewStub
- Replaced all `<View>` with `<TextView android:text="">` in `hbot_device_widget.xml`
- **This was the crash for builds 119-121** — all the other fixes (class resolution, R.id, etc.) were red herrings

### Alexa App-to-App Account Linking
- Amazon's documented OAuth flow (same as Tuya uses) for "Link X with Alexa" consent screen
- Created `lib/services/alexa_account_linking_service.dart` with PKCE (S256) via `crypto` package
- Consent URL: `https://alexa.amazon.com/spa/skill-account-linking-consent`
- Fallback: `https://www.amazon.com/ap/oa` (LWA direct)
- Requires LWA Security Profile `client_id` from Amazon Developer Console (`--dart-define=ALEXA_LWA_CLIENT_ID`)
- Profile screen uses account linking service with instruction sheet fallback until configured
- Redirect URI: `https://h-bot.tech/alexa/callback`
- **Blocked**: Amir needs to create LWA Security Profile and provide `client_id`

## Build 123 (commit `e5cc98e`) — Redesigned widget with dark glass UI
### Design overhaul
- Dark semi-transparent background (`#DD1A1A2E`, 16dp rounded corners)
- H-Bot app icon in header with "X ON" status counter
- Device type icons: light bulb, power switch, shutter blinds (vector drawables)
- Green pill (`#10B981`) toggle for ON, translucent white (`#33FFFFFF`) for OFF
- Brighter device names when ON, dimmer when OFF
- Thin white divider separating header from devices
- Empty state: "Open H-Bot to load devices"

### Toggle functionality (HomeWidgetBackgroundIntent approach)
- Toggle buttons fire `HomeWidgetBackgroundIntent` → Dart background callback
- Callback updates widget data immediately for visual feedback, stores pending MQTT command
- Dashboard processes pending toggles via `_processPendingWidgetToggle()`
- **Problem**: Background callback runs in dead isolate — no network, no MQTT → toggles don't actually fire

### Files created
- `android/app/src/main/res/drawable/widget_background.xml`
- `android/app/src/main/res/drawable/toggle_on_bg.xml`, `toggle_off_bg.xml`
- `android/app/src/main/res/drawable/device_icon_light.xml`, `device_icon_switch.xml`, `device_icon_shutter.xml`

## Build 124 (commit `6946314`) — Attempted launch intent for toggle
- Switched to `HomeWidgetLaunchIntent` — app opens when user taps toggle
- Dashboard listens to `HomeWidget.widgetClicked` stream + `initiallyLaunchedFromHomeWidget`
- `_handleWidgetUri` parses `hbot://toggle?deviceId=X&state=ON/OFF`
- `_executeWidgetToggle` sends MQTT via `_mqttManager.mqttService.sendBulkPowerCommand()`
- **Tamer feedback**: looks much better but toggle still does nothing (opening app is bad UX)

## Build 125 (commit `abf3315`) — Direct MQTT from widget background
### Widget actions without opening app
- Background callback (`homeWidgetBackgroundCallback`) now connects to MQTT broker directly:
  - Creates `MqttServerClient` to EMQX broker with TLS
  - Embedded CA cert as string constant (rootBundle unavailable in background isolate)
  - Authenticates as `admin`/`P@ssword1`, sends command, disconnects
  - Falls back to pending queue if MQTT connection fails
- Imports added to `main.dart`: `dart:convert`, `dart:io`, `mqtt_client`, `mqtt_server_client`

### Shutter support
- Shutters show ▲/▼ arrows instead of ON/OFF toggle
- `hbot://shutter?deviceId=X&topic=Y&direction=up/down` URI scheme
- Sends `cmnd/$topic/ShutterPosition1` with `0` (up/open) or `100` (down/close)

### Widget data now includes
- `device_{i}_topic` — MQTT topic base for direct command sending
- `device_{i}_channels` — channel count
- `WidgetDevice` class updated with `topicBase` and `channels` fields

### Layout improvements
- Bigger toggle buttons: 52×30dp (was 44×24dp)
- Larger device icons: 22dp
- Better padding and spacing
- Shutter rows: icon + name + ▲ + ▼ buttons
- Non-shutter rows: icon + name + ON/OFF pill

### Key lesson
- `HomeWidgetBackgroundIntent` DOES work for network operations — just need to initialize a fresh MQTT client in the callback since the isolate has no shared state
- The `SecurityContext` works with `setTrustedCertificatesBytes(utf8.encode(...))` for embedded cert strings

### Awaiting Tamer's test of Build 125

## Build 126 (commit `c32476a`) — Native MQTT widget toggles + device picker
- Rewrote widget toggle to use **native Kotlin BroadcastReceiver** (`WidgetToggleReceiver.kt`)
- Uses Eclipse Paho MQTT client (Java library) — no Flutter engine involved
- Added `org.eclipse.paho:org.eclipse.paho.client.mqttv3:1.2.5` to `build.gradle.kts`
- `WidgetToggleReceiver` connects to EMQX with TLS, sends `POWER0 ON/OFF` or `ShutterPosition1`, disconnects
- Embedded CA cert as string constant in Kotlin (same approach as Dart background isolate)
- EMQX Cloud HTTP API (port 8443/8080/8083) NOT available on Serverless tier — only MQTT 8883
- `HBotDeviceWidget.kt` rewritten to use native `PendingIntent.getBroadcast()` for toggle actions
- Added ProGuard rules for Paho and WidgetToggleReceiver
- Created `lib/screens/widget_config_screen.dart` (Flutter-side config, later replaced by native)

## Build 127 (commit `21e29ce`) — Native widget config activity
- Tamer requested: widget config should open when adding widget (like standard Android widgets), NOT from app settings
- Created `WidgetConfigActivity.kt` — pure native Android UI (no Flutter engine)
  - Dark themed (matches app: `#1A1A2E` bg, `#252540` cards, `#10B981` accent)
  - Reads all devices from `HomeWidgetPreferences` SharedPreferences (`all_devices_json` key)
  - Shows emoji icons (💡/⚡/🪟), device names, type + channel count
  - Select up to 4 devices with numbered green badges
  - Saves to SharedPreferences: `device_{i}_*` slots + `widget_favorites` JSON
- Set `android:configure="com.example.hbot.WidgetConfigActivity"` in widget XML
- Registered activity in AndroidManifest with `Theme.Translucent.NoTitleBar`
- Dashboard now calls `HomeWidgetService.saveAllDevicesForConfig()` to store all devices
- Removed widget config from Profile → Integrations settings
- Key: user must open app at least once to populate device list before adding widget

## Alexa LWA Client ID Configured
- Tamer provided: `amzn1.application-oa2-client.cd1600c814ee4ca3bc6030c550980529`
- Hardcoded as `defaultValue` in `AlexaAccountLinkingService.clientId`
- Amir completed skill-side account linking setup
- `isConfigured` now returns `true`

## Build 128 (commit `af38059`) — False offline notification fix
- **Root cause**: App goes to background → MQTT disconnects → all devices emit `online: false` → notification spam
- **Fix**: `DeviceEventTracker` now uses `WidgetsBindingObserver` for lifecycle awareness
  - `_isInForeground` flag — suppresses offline notifications when app is backgrounded
  - 60-second `_reconnectGrace` period after app resume (MQTT reconnecting)
  - Clears all `_lastOnline` states on resume (fresh start)
  - Increased debounce from 30s to 2 minutes
  - Online notifications still fire normally

## Build 129 (commit `d874b2c`) — Alexa linking opens LWA consent page
- Previous: tried `alexa.amazon.com/spa/skill-account-linking-consent` — doesn't deep-link from third-party apps
- Fixed: now opens `amazon.com/ap/oa` (LWA OAuth) directly in browser with correct params
- Same approach as Tuya uses
- Removed the dead Alexa app deep-link attempt (was never working)
- Tamer testing this build

### Pending
- Test widget config activity with real devices
- Test native MQTT toggle from widget
- Test Alexa account linking end-to-end
- iOS widget extension not yet created
