# 2026-03-17 — Session Notes

## Build 137 — Google sign-in removed
- Removed Google sign-in button + "or" divider from both `sign_in_screen.dart` and `sign_up_screen.dart`
- Email/password auth only now
- Commit: `b3b7426` on `hbot-design`
- APK sent to Tamer, pushed to git
- Tamer said: **Don't push to TestFlight** unless asked — APK + git only going forward

## App Store Submission Progress (Version 1.0, Build 136)

### Completed via API
- App name: **H-Bot Smart Home** (plain "H-Bot" was taken)
- Subtitle: **Smart Home Control**
- Description, keywords (smart home, IoT, home automation, etc.)
- Support/Marketing URL: `https://h-bot.tech`
- Privacy policy: `https://aoperatingsystem.online/hbot-privacy` (static HTML served via nginx)
- Screenshots uploaded: iPhone 6.7" (1290×2796), 5.5" (1242×2208), iPad Pro 12.9" (2048×2732) — 5 each
  - Dashboard, Scenes, Device Control, Widgets, Security/Features
  - **Important**: 6.7" screenshots must NOT have alpha channel (Apple rejects PNG with alpha → use `PNG24:` output)
- Age rating: all NONE
- Content rights: DOES_NOT_USE_THIRD_PARTY_CONTENT
- Copyright: 2026 Momentum Solutions
- Category: LIFESTYLE (primary), UTILITIES (secondary)
- Pricing: FREE (price point `eyJzIjoiNjc2MDI1MzA1NCIsInQiOiJVU0EiLCJwIjoiMTAwMDAifQ`)
- Review detail created (ID: `24eff6c9-8eef-49e8-8234-79e2d75c9b3b`)
  - Demo account: `test@hbot.app` / `TestReview2026!`
  - Contact: Tamer Ishak, support@msolapps.store
- iPad screenshot set ID: `be3429f3-c438-4d09-b811-188a3bc5180c`

### Blocked — Needs manual action in App Store Connect
1. **App Privacy questionnaire** — cannot be done via API (endpoint doesn't exist). Must go to ASC web → App Privacy → answer data collection Qs (email for account, device ID for functionality, rest = no)
2. **Demo account creation** — `test@hbot.app` signup failed (Supabase email confirmation error). Need to create/confirm manually in Supabase dashboard or use existing account
3. After those two: can submit for review via `reviewSubmissions` API

### Review Submission IDs (stale, need fresh one after fixes)
- `a2ff4849-218c-4762-aac1-d6d722532fe3` — failed (screenshot alpha issue)
- `351312a7-8c48-4277-b3f2-95123ecd2870` — failed (missing iPad)
- `bccb2099-2d6c-4962-afcd-0c23a0dfb8a6` — failed (missing privacy + demo account)

### ASC API Lessons
- `appStoreVersionSubmissions` (v1) is deprecated — use `reviewSubmissions` + `reviewSubmissionItems`
- App Privacy / data usages have NO API endpoint — must use ASC web interface
- `appPriceSchedules` needs `included` array with inline `appPrices` referencing price point ID
- iPad Pro 12.9" 3rd gen screenshots (`APP_IPAD_PRO_3GEN_129`) are REQUIRED for submission
- Screenshots with alpha channel → `IMAGE_ALPHA_NOT_ALLOWED` error; use `convert -alpha off PNG24:output.png`
- `contentRightsDeclaration` goes on `/v1/apps/{id}` not `/v1/appInfos/{id}`

## Custom Email Verification — Supabase Custom SMTP (Build 138)

### What was done
- Reverted custom OTP Edge Function approach → using Supabase's built-in SMTP relay instead
- Created branded HTML email templates: `assets/email-templates/confirm-signup.html`, `reset-password.html`, `magic-link.html`
  - H-Bot dark theme: bg `#010510`, card `#1A202B`, accent `#0883FD`
  - `{{ .Token }}` placeholder for 6-digit OTP
- Updated `auth_repo.dart`: removed custom `EmailOtpService` dependency, uses Supabase Auth native `verifyOTP`, `resend`, `resetPasswordForEmail`
- Forgot password screen now uses OTP code entry (not magic link)
- `email_otp_service.dart` still exists but is no longer imported by auth_repo
- Commit: `ea9216f` (code) + `6fb3bd6` (APK), branch `hbot-design`
- Build 138 APK sent to Tamer

### Supabase SMTP Config via Management API
- Used `PATCH /v1/projects/{ref}/config/auth` with Supabase access token
- Token: `sbp_a185e8edadded0ecab38f42be9c08138d5b73682`
- Configured: `mail.privateemail.com:465`, user `info@h-bot.tech`, sender `H-Bot Smart Home`
- OTP settings: 6 digits, 600s (10 min) expiry
- Branded subjects: "H-Bot: Verify Your Email", "H-Bot: Reset Your Password"

### ⚠️ SMTP Auth Failure — BLOCKING
- Namecheap app password `nxuy-zjtf-rdxv-sfgu` fails authentication
- Tried: port 465 (SSL), port 587 (STARTTLS), password with and without dashes — all fail
- Signup returns: `500 unexpected_failure "Error sending confirmation email"`
- **Gmail SMTP also broken**: original password was `5d79fb9dc314023f72d1b853ed84fa69156ccaf1bc7969c8857b19cb3e5ee5b0` (encrypted form from API readback, NOT the actual password)
- Passing encrypted password back to API doesn't work → need original Gmail app password to restore
- **Currently SMTP is set to Namecheap (broken) — no emails sending at all**
- Need from Tamer: correct Namecheap password OR original Gmail app password for `hbot.smarttech@gmail.com`

### Supabase Management API Lessons
- `PATCH` with partial fields WIPES unset SMTP fields to null — always send ALL smtp_* fields together
- `smtp_pass` returned by GET is encrypted hash, cannot be reused as password
- Cloudflare blocks requests without `User-Agent` header (error 1010)
- Tamer feedback: "always tell me what you're going to do before executing" — plan first for architectural tasks

### Missing "Forgot Password" button
- Code has it in `sign_in_screen.dart` — Tamer may not have installed Build 138 yet
- Screenshots showed light theme (likely older build)

## SMTP Fixed — Real Password Found (Build 139)
- Namecheap **real password `02Feb@2016`** works (NOT the app password)
- Supabase rate limits bumped: `rate_limit_email_sent: 30`, `smtp_max_frequency: 30s`
- Email logo hosted at `https://aoperatingsystem.online/hbot-email-logo.png`
- Profile avatar changed from `😀` emoji to `Icons.person`
- Commit: `48ebed6`, APK: `182155f`

## Build 140 — Reset Password Dark Theme Fix
- Reset password screen fully rewritten with dark theme (`context.hBackground`, `context.hCard`, `context.hTextPrimary`)
- OTP boxes: dark fill, themed border, white text
- Password fields use `SmartInputField` component (consistent with login/signup)
- Button uses `hbotPrimaryButtonDecoration`
- Commit: `80d85c7`
- Tamer confirmed: forgot password is fixed ✅

## Build 141 — Delete Account + Email Logo Fix
- **Delete Account** added to profile screen (below Sign Out)
  - Red icon + text, confirmation dialog with permanent deletion warning
  - Calls existing `delete_user_account` Supabase RPC
  - Apple requires delete account for App Store compliance
- Email logo replaced: server now serves clean blue "H" icon (ImageMagick-generated, 200x200, 2KB)
- Email templates updated in Supabase with `border-radius:50%` (round icon)
- Commit: `812e7c5`, APK: `5f182df` (pushed + sent to Tamer)

## Build 142 — App Store Submission Prep
- Bumped version to `1.0.0+142` for iOS build
- Previous Codemagic build (141) failed: same build number 137 still in pubspec
- **Privacy policy updated**: email changed to `info@h-bot.tech`
- **Review contact updated**: `info@h-bot.tech`
- **ASC URLs updated**: support + marketing → `https://h-bot.tech`
- All old screenshots deleted, 15 new screenshots uploaded (dashboard, scenes, device control, profile, login × 3 sizes)
- Tamer completed App Privacy questionnaire manually ✅
- Codemagic build `69b95fa4635e8540f53a8c48` triggered — status: building at 14:16 UTC
- Commit: `8c11596`

### Pending After Codemagic Build Lands
1. Wait for Build 142 to appear on TestFlight (processing ~5-10 min after upload)
2. Set export compliance (`usesNonExemptEncryption: false`)
3. Attach build to version `b094dc6c-22c0-46e9-8df2-30a768cecef6`
4. Create `reviewSubmissions` + `reviewSubmissionItems`
5. Confirm submission

### Email Logo Issue
- Tamer keeps seeing droplet icon — those are OLD emails (cached in inbox)
- Server now serves clean blue "H" rounded square at `/hbot-email-logo.png`
- New emails from Supabase will fetch the updated logo
- Nginx route: `location ~ ^/hbot-email-logo.*\.png$` → `/var/www/html/`

## App Store — Real Screenshots + MQTT/Tasmota Cleanup
- Tamer sent 6 real screenshots from the app (881x1280 source images)
- Resized to 3 ASC sizes: 1290x2796 (6.7"), 1242x2208 (5.5"), 2048x2732 (iPad 12.9")
- Had to cancel review submission twice to edit (can't modify while WAITING_FOR_REVIEW)
- **OLD mockup screenshots persisted** despite DELETE calls — deletes don't take effect while in review
- After cancellation to DEVELOPER_REJECTED, successfully deleted 15 old mockups + uploaded 5 real per set
- 5 screenshots per set (ASC max): dashboard, device, scenes, profile, login

### GOLDEN RULE: No MQTT/Tasmota anywhere user-facing
- Tamer reminded: "remember our golden rule tasmota and mqtt shouldn't show anywhere"
- Cleaned from ASC: description, keywords, review notes
- Keywords now: `smart home,IoT,home automation,lights,switches,shutters,remote control,scenes,devices,home control`
- Description: "real-time status updates" (removed "via MQTT"), "works with a wide range of smart home devices" (removed Tasmota/MQTT)
- Review notes: "a range of IoT devices" (removed Tasmota/MQTT)
- **Must remember this for ALL future listings (Play Store, marketing, etc.)**

### Active Submission
- Submission ID: latest one created after screenshot + text cleanup
- State: WAITING_FOR_REVIEW (Build 142, version 1.0)

## Google Play Store — Setup In Progress
- Tim wants full API automation (Option A)
- AAB built: `build/app/outputs/bundle/release/app-release.aab` (42.3MB)
- Tim has Google Play developer account (ID: `6376157508824598411`, 23 existing apps)
- Tim completed: GCP project creation, API enabled, service account created, JSON key downloaded, invited to Play Console
- **Waiting for**: Tim to send the service account JSON key file
- Bundle ID for Android: `com.msol.hbot`
- Play Console needs app created first (can't be done via API)

## Google Play — API Setup Complete (~15:30 UTC)

### Package name change
- `store.msolapps.hbot` conflicted with existing app on Tim's Play account
- Changed to `tech.hbot.app` in `build.gradle.kts` (namespace stays `com.example.hbot`)
- New release keystore: `android/upload-keystore.jks` (pass: `hbot2026release`, alias: `upload`, CN=Momentum Solutions, OU=Mobile, O=Momentum Solutions, L=Dubai, ST=Dubai, C=AE)
- `android/key.properties` → `storeFile=../upload-keystore.jks`

### Service account
- Email: `hbot-play-api@hbot-play.iam.gserviceaccount.com`
- Key: `/root/.playstore/service-account.json` (private key ID: `c55cd05cdc72d0638c2bb05167a4ef658520fb76`)
- GCP project: `hbot-play`
- Tim assigned 13 permissions manually in Play Console

### What was done via API
- Store listing: title "H-Bot Smart Home", descriptions (MQTT/Tasmota-free), en-US
- 5 phone screenshots + 5 tablet (10-inch) screenshots uploaded
- App icon 512×512 (upscaled from xxxhdpi launcher icon)
- Feature graphic 1024×500 (generated: blue gradient + white "H-Bot Smart Home" text)
- Contact details: email `info@h-bot.tech`, website `https://h-bot.tech`
- Internal testing track set to draft, version code 142

### API commit lesson
- First commit failed 400 — "completed" internal release (from manual save) conflicted
- Fix: set track to draft-only in a new edit, then commit succeeds
- Tim had to manually "Save and publish" the internal release first to unlock API

### Still needs manual action in Play Console
1. **App content** section (left sidebar → Policy or Dashboard checklist):
   - Privacy policy URL: `https://aoperatingsystem.online/hbot-privacy`
   - App access: all functionality without special access
   - Ads: no ads
   - Content rating: IARC questionnaire
   - Target audience: 13+ (not for children)
   - Data safety: collects email (account management), device identifiers (app functionality)
2. **`dataSafety` API endpoint returned 404** despite appearing in API discovery — confirmed NOT functional
3. Content rating, data safety, privacy policy, target audience → all manual-only in Play Console

### Google Play API Lessons
- `dataSafety` resource exists in discovery JSON but 404s on actual calls — not usable
- First AAB upload MUST be manual in Play Console — API unlocks after
- Commit fails if track has a "completed" release status from manual publish
- Feature graphic and app icon go under `images` resource (type `featureGraphic`, `icon`)

## 2026-03-17 21:00 UTC — Wave 1-3 Execution Complete

### Build 143 — All 13 Tasks Completed
Committed as `9c5fc22` on `hbot-design` branch.

#### Wave 1 (sub-agents, completed ~18:48):
- #3: Removed "Skip for now" from email verification
- #5: OTP paste support (6-digit fills all fields)
- #6: Removed duplicate Delete Account from profile
- #8: Alexa button fixed (1 line title, steps only)
- #9: Activity Log removed from profile
- #13: Build indicator moved from dashboard to profile footer
- #1: All On/Off now uses bulk POWER0 command

#### Wave 2 (sub-agents, completed ~18:57):
- #10.1: Scenes dark mode fixed (12 hardcoded colors → theme-aware)
- #4: Change Password with OTP in HBot Account (3-step flow)
- #10.2: Shared devices appear in scene creation (orange "Shared" badge)
- #11: Auto-create "My Home" for shared device recipients

#### Wave 3 (completed ~21:00):
- #12: Arabic/RTL localization — 200+ string pairs, LocaleService, language switcher
  - Arabic sub-agent timed out at 10min but completed ~90% — I finished sign_in + sign_up manually
  - 215 AppStrings.get() calls across 12 screens
- #2: Admin panel deployed at https://aoperatingsystem.online/hbot/
  - Login: info@h-bot.tech / 02Feb@2026 (Supabase user created)
  - Nginx location added: `/hbot` → `/var/www/html/hbot/`
- In-app notification center: BroadcastService + NotificationsInboxScreen + bell badge

#### Wave 4:
- #7: Email logo updated to hbot branding icon (200x200) at /hbot-email-logo.png

### Admin Panel Supabase Setup
- Admin user: info@h-bot.tech (ID: 9f7b75e8-caa6-4435-823f-30eb06f11707)
- Table: broadcast_notifications (already existed)
- Admin panel uses client-side auth hash + Supabase password auth for API token
- Panel fetches stats from profiles + broadcast_notifications tables

### Arabic Localization Architecture
- `lib/l10n/app_strings.dart` — static Map<String, Map<String, String>> approach (no intl package)
- `lib/services/locale_service.dart` — ChangeNotifier, SharedPreferences
- `AppStrings.get('key')` pattern for string lookup
- flutter_localizations SDK package added
- RTL automatic via MaterialApp locale property
