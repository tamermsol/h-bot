# 2026-03-18

## Arabic Localization — Complete (09:34 UTC)

### What was done
- Extracted 356 strings from 24 files using `scripts/localize_all.py`
- Sub-agent `ar-translate` translated 180 Arabic strings → `scripts/arabic_fixes.json`
- Applied all strings in single pass via `scripts/apply_localization.py`
- 330+ new keys added to `lib/l10n/app_strings.dart` (en + ar maps)
- 231 string replacements across 22 files
- Fixed all `const_eval_method_invocation` errors (AppStrings.get() can't be in const widgets)
- Removed 13 problematic keys with `$e`/`${e.toString()}` interpolation (kept as hardcoded in screens)
- Zero compile errors after cleanup
- Commit: `3d6f38c` on `hbot-design`, pushed

### Files localized (20 screens + 3 widgets)
- Screens: activity_log, add_device_flow, add_timer, devices, device_timers, feedback, help_center, homes, multi_device_share, notifications_settings, profile_edit, reset_password, rooms, scan_device_qr, shared_devices, share_device, shutter_calibration, shutter_manual_calibration, wifi_profile
- Widgets: device_selector, shutter_control_widget, background_image_picker

### Localization scripts (in scripts/)
- `localize_all.py` — extracts strings from Dart files → JSON
- `apply_localization.py` — merges arabic_fixes.json + applies to codebase
- `localization_strings.json` — all 356 extracted strings with en/ar
- `arabic_fixes.json` — 180 manual Arabic translations
- `new_strings.dart` — generated Dart output (reference)

### Still needs localization
- Partially-localized screens (~300+ hardcoded strings remaining): home_dashboard_screen, profile_screen, device_control_screen, add_scene_screen, scenes_screen
- Services with user-facing strings

### Killed stale sub-agents
- ar-devices, ar-rooms, ar-feedback sub-agents completed but were producing full screen rewrites (not useful) — results ignored, NO_REPLY sent

### Pending
- FCM setup — waiting for Tim's Firebase config files
- App Store review status for build 143
- Google Play first-app review (2-7 days)
- Full QA testing before any store publish

## FCM Push Notifications — Setup Complete (12:40 UTC)

### Server-side
- Firebase Admin SDK saved: `/root/.firebase/firebase-admin-sdk.json`
- APNs key saved: `/root/.firebase/AuthKey_7KXU88V423.p8`
- Firebase project: `hbot-app-6c521` (HBOT APP)
- FCM API server: systemd service `hbot-fcm` on port 8099
- Nginx proxy: `/hbot/api/` → `127.0.0.1:8099`
- API endpoints: `/hbot/api/push` (POST), `/hbot/api/token-count` (GET), `/hbot/api/health` (GET)
- Admin panel updated: sends FCM push + saves to broadcast_notifications
- APNs Key ID: `7KXU88V423`, Team ID: `6U3ELYT3M7` — uploaded to Firebase Console by Tim

### Flutter client
- Added `firebase_core` + `firebase_messaging` packages
- Created `lib/services/fcm_service.dart` — handles token registration, permissions, background/foreground handlers
- `Firebase.initializeApp()` in `main.dart`
- `FcmService().initialize()` called in `HomeScreen.initState()` (after auth)
- Token auto-registered to `fcm_tokens` table in Supabase
- Supabase table: `fcm_tokens` with RLS (users manage own, service_role full access)

### Confirmed working
- Tim confirmed "PNs works" ✅

## Shared Devices Fix (13:00 UTC)
- Root cause: `listSharedDevices()` was joining `shared_devices` → `devices_with_channels(*)` in one query, but RLS on the `devices` table was blocking the join for non-owner users
- Fix: Two-step query — first get `device_id` list from `shared_devices`, then fetch from `devices_with_channels` using `.inFilter('id', deviceIds)` which is allowed by the "Users can view shared devices" RLS policy
- Commit: `1e5fbf2`

## Scene Localization — Batch 3 (13:00 UTC)
- Added ~50 more localization keys for add_scene_screen
- Fixed: triggers, repeat options, days, location, device selection, action config, review, error messages
- Fixed: مشهد → سيناريو everywhere
- Fixed: Enabled/Disabled → مفعّل/معطّل
- Fixed: Next/Previous/Create Scene buttons
- All const errors fixed (removed const from SnackBars using AppStrings)
- Commit: `1e5fbf2`

## Notifications Read Bug Fix
- Root cause: Supabase `broadcast_notifications` table had no UPDATE RLS policy
- Fix: Added `"Authenticated users can mark as read"` UPDATE policy
- Now `markAsRead()` successfully updates `read_by` array

## Shared Devices Debug (15:00 UTC)
- RPC `get_shared_devices()` works correctly server-side (tested with real auth tokens)
- Test user login + share + RPC call all return correct device
- Tamer (`tamer.s.ishak@gmail.com`) confirmed correct login account
- Triple-fallback approach deployed: RPC → direct `devices` table query → join fallback
- Commit `4ac548f` — still not working for Tamer; will test with different account/device later
- Shared device is with user `666bd8fe...` (correct), device `c2cfa1da...` ("Hbot Shutter")

## Localization Batch Fix (15:13 UTC)
- Fixed 7 screens with ~42 new Arabic keys: notifications settings, feedback, profile edit, homes, rooms, change password dialog, view & filter options
- Commit `5016bab`
- Two Python scripts: `scripts/fix_screens_l10n.py` + `scripts/fix_screens_l10n2.py`

## Tamer Feedback Collection (15:40-15:55 UTC) — WAITING FOR "GO"
Tamer said: collect all feedback first, then fix in one batch when he says "go"

### Collected Issues:
1. **View & Filter Options** — title still English (may be old APK, fix was deployed)
2. **Push notifications toggle always OFF** — even after accepting permission prompt; toggle doesn't reflect actual state
3. **Phone numbers / URLs reversed in RTL** — phone shows `81167100 12 20+` instead of `+20 12 81167100`; URL shows `/https://h-bot.tech`. Need `Directionality(textDirection: TextDirection.ltr)` wrapper
4. **Scene "Select Devices" button dark mode** — blue button with invisible/dark text, needs white text color
5. **Notifications inbox dark mode** — cards have white/light background, need dark surface color
6. **Homes menu subtitle** — change from "إضافة وتعديل وحذف المنازل" to just "المنازل"
7. **Hide location trigger from scenes** — for initial store release (avoid background location permission review issues). Also remove geolocator dependency and location permissions from manifest/plist
8. **Admin panel — Recent Broadcasts not loading** — shows "No notifications sent yet"
9. **Admin panel — Bilingual notifications** — need separate AR/EN title+body fields, send based on user's locale
10. **Admin panel — Image attachment** — ability to attach image URL to FCM notification
