# 2026-03-18

## Arabic Localization — Complete (09:34 UTC)

### What was done
- Extracted 356 strings from 24 files using `scripts/localize_all.py`
- Sub-agent `ar-translate` translated 180 Arabic strings → `scripts/arabic_fixes.json`
- Applied all strings in single pass via `scripts/apply_localization.py`
- 330+ new keys added to `lib/l10n/app_strings.dart` (en + ar maps)
- 231 string replacements across 22 files
- Fixed all `const_eval_method_invocation` errors (AppStrings.get() can't be in const widgets)
- Removed 13 problematic keys with `$e`/`${e.toString()}` interpolation (kept as hardcoded in screens)
- Zero compile errors after cleanup
- Commit: `3d6f38c` on `hbot-design`, pushed

### Files localized (20 screens + 3 widgets)
- Screens: activity_log, add_device_flow, add_timer, devices, device_timers, feedback, help_center, homes, multi_device_share, notifications_settings, profile_edit, reset_password, rooms, scan_device_qr, shared_devices, share_device, shutter_calibration, shutter_manual_calibration, wifi_profile
- Widgets: device_selector, shutter_control_widget, background_image_picker

### Localization scripts (in scripts/)
- `localize_all.py` — extracts strings from Dart files → JSON
- `apply_localization.py` — merges arabic_fixes.json + applies to codebase
- `localization_strings.json` — all 356 extracted strings with en/ar
- `arabic_fixes.json` — 180 manual Arabic translations
- `new_strings.dart` — generated Dart output (reference)

### Still needs localization
- Partially-localized screens (~300+ hardcoded strings remaining): home_dashboard_screen, profile_screen, device_control_screen, add_scene_screen, scenes_screen
- Services with user-facing strings

### Killed stale sub-agents
- ar-devices, ar-rooms, ar-feedback sub-agents completed but were producing full screen rewrites (not useful) — results ignored, NO_REPLY sent

### Pending
- FCM setup — waiting for Tim's Firebase config files
- App Store review status for build 143
- Google Play first-app review (2-7 days)
- Full QA testing before any store publish
